package moo.proyecto.entrega2;

/**
 * Interfaz que representa cada uno de los poderes que un Item puede tener.
 * En principio es una interfaz sin métodos, que se usa para "etiquetar" cualquiera
 * de los poderes (implementados como subclases) y así poder guardarlos en un mismo
 * contenedor.
 */
public interface PoderItem {
}
